# Session Record — SES-2026-005

## Document Control
| | |
|---|---|
| Document | Session Record |
| Reference | SES-2026-005 |
| Project | MyDesign |
| Version range | v4.1.0 |
| Owner | Abdulmohsen |
| Date | 2026-07-01 |
| Status | Shipped, deployed, committed |

## 1. Summary

A small follow-on to v4.0.0: **unified email-or-phone login** (one portal for clients *and* team) and **onboarded the real design team** as staff accounts.

## 2. Versions shipped

**v4.1.0 · Email-or-phone login + team staff accounts**
What: `/auth/login` now matches by **email OR phone** for both `admins` and `clients` (done as two safe `.eq()` lookups — phone then email — never a string-built `.or()`, which would be injectable given the default passwords). `admins` gained an `email` column and `phone` became optional; the **Staff** page now captures email (phone optional). Seeded the team: **Tuqa** (manager / team-lead), **Hiba · Mohammed · Esra · Raneem** (designers), all `@mysaudi.co`, default password `123123`.
Why: the team logs in with work emails; clients keep phone — one portal, either identifier.
Schema: migration `0003_staff_email_login`.
Decision: MINOR.

## 3. Schema changes

Migration `0003_staff_email_login` (additive), applied to prod: `admins.email` (text); `admins.phone` set nullable (email-only staff); unique index on `lower(email)`.

## 4. Live test record

- **tsc PASS · next build PASS** (25 routes).
- **Login lookups verified via SQL:** Tuqa & Esra by email resolve to the right roles (manager / designer); owner by phone still works; a wrong password returns no row.
- **Deployed** `1df2d06` (tag v4.1.0), aliased to https://mydesign-blush.vercel.app.

## 5. Open items

- **Tuqa → `manager`** (the only oversight role — full access incl. Staff + Pricing). If she shouldn't manage staff or set pricing, add a dedicated `team_leader` role.
- **Mohammed's email inferred** as `mohammed@mysaudi.co` (pattern) — confirm.
- All new staff use the **default password `123123`** — have them change it (no change-password UI yet).
- **Security Phase 2** remains the priority: roles + the internal price are client-side only.

## Appendix — version → commit map

`… · v4.0.0 de19920 · v4.1.0 1df2d06 (tag v4.1.0)`
