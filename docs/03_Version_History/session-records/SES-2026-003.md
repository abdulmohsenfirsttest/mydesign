# Session Record — SES-2026-003

## Document Control
| | |
|---|---|
| Document | Session Record |
| Reference | SES-2026-003 |
| Project | MyDesign |
| Version range | v3.4.0 |
| Owner | Abdulmohsen |
| Date | 2026-06-30 |
| Status | Shipped (operational tooling; no app/runtime change) |

## 1. Summary

Stood up MyDesign's **operational backbone**, mirroring the Favor Plus setup: an **automated nightly Supabase backup** to Google Drive, a **bug log**, and a **backups doc** — tying together the existing CODE_HEALTH + SECURITY discipline. No product/app code changed; this is tooling + documentation, versioned **v3.4.0**.

## 2. Versions shipped

**v3.4.0 · Operational backbone — backups + bug log**
What: (a) automated DB backups — `scripts/backup-data.mjs` (JSON-per-table + manifest, 90-day retention, service-role-preferred with anon fallback) + `scripts/run-backup.sh` (data + a `git archive` source zip) + launchd `com.mydesign.backup` (daily 02:00) → Google Drive `mydesign/Backups` + `mydesign/Code`; (b) `docs/04_Code_Health_&_Security/BUGLOG.md` (seeded with 6 bugs + a watch list); (c) `docs/04_Code_Health_&_Security/BACKUPS.md`; (d) the CODE_HEALTH weekly routine now verifies the backup + records bugs.
Why: bring MyDesign to the same operational maturity as Favor Plus / PHSN (versioning + session records already existed).
Schema: none.
Decision: MINOR — a batch of operational tooling, no runtime change. The backup uses the anon key for now (works only because RLS is off); add a service-role key before the RLS Phase-2 lockdown.

## 3. Schema changes

None.

## 4. Architectural decisions

- **Backup mirrors Favor Plus** (`backup-data.mjs` + launchd + Drive), adapted to MyDesign: prefers `SUPABASE_SERVICE_ROLE_KEY`, falls back to the anon key, and uses a known-table list because the anon key returns an empty PostgREST OpenAPI spec.
- **One Drive folder per project:** backups + the source zip live under `My Drive/mydesign/` next to the docs mirror.

## 5. Live test record

- Backup run manually **and** via the launchd wrapper: **6 rows across 9 tables**, one JSON per table + `manifest.json` written to Drive; `git archive` source zip (~21 MB) written to `mydesign/Code`; `exit: 0`; `LATEST.txt` updated.
- launchd job `com.mydesign.backup` installed at `~/Library/LaunchAgents/` and **loaded** (daily 02:00, RunAtLoad).
- App unchanged — no build/deploy needed (scripts + docs only). `next build` / `tsc --noEmit` remain green from v3.3.0.

## 6. Health scan

- **Backups:** ✅ live & verified (`LATEST.txt` + `backup.log` exit 0; job loaded).
- **Docs:** added `BUGLOG.md` + `BACKUPS.md` under `04_Code_Health_&_Security`.
- **Security:** unchanged. ⚠️ The backup currently reads via the anon key (works only because RLS is off) — add `SUPABASE_SERVICE_ROLE_KEY` to `.env.local` before Security Phase 2 or it will miss rows on RLS-protected tables.
- **Version control:** committed as v3.4.0, pushed.

## 7. Open items

(carried from SES-2026-002, plus the backup key item)
- **Add `SUPABASE_SERVICE_ROLE_KEY`** to `.env.local` so backups survive the RLS lockdown.
- **Meeting-3 Increment 2** (client-portal display of spaces/dates) + the §6 Meeting-3 decisions (roles / internal pricing / proposal / bundling).
- **Security Phase 1** (rotate the anon key, restrict the `admins` table) → **Phase 2** (RLS incl. `spaces`, server-side validation, hash passwords, private buckets).
- Generate `supabase/migrations/0000_baseline.sql` via `supabase db dump`.
- Add tests; fix the remaining `app/dashboard/*` lint errors, then flip CI lint to blocking.

## Appendix — version → commit map

`… · v3.2.0 7e3c7c8 · v3.3.0 7e3c7c8 · v3.4.0 08e9958 (tag v3.4.0)`
